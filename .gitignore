target/

# Maven wrapper cache
.mvn/wrapper/maven-wrapper.jar

# IDE files
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Local env files with secrets (use .env.example as a template).
# Never commit the real `.env` — it contains the Bunny storage API key.
.env
.env.local
.env.*.local
application-local.properties
